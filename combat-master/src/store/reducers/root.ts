const initialState = {
  selectedMovement: 0,
  selectedAction: "",
  selectedBonusAction: "",
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
